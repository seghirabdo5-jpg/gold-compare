/**
 * ============================================================
 *  ملف الإعدادات المركزي للموقع
 *  عدّل القيم هنا فقط، ولا تحتاج لتعديل أي صفحة أخرى.
 * ============================================================
 */

export const siteConfig = {
  // اسم الموقع (يظهر في الشعار، العنوان، والـ SEO)
  name: "سعر الذهب اليوم",

  // وصف قصير للموقع يظهر في محركات البحث
  description:
    "تابع أسعار الذهب اليوم في السعودية، احسب قيمة الذهب، وقارن بين أفضل متاجر الذهب والمجوهرات.",

  // الدومين النهائي للموقع (غيّره بعد شراء الدومين، وإلا اترك رابط Vercel المؤقت)
  url: "https://your-site.vercel.app",

  // الدولة الأساسية المستهدفة الآن
  primaryCountry: "SA",

  // عملة الموقع الأساسية
  currency: "SAR",

  // رقم واتساب (اختياري) - اتركه فارغًا "" إن لم يتوفر
  whatsappNumber: "",

  // ---------------- تتبع وتحليلات ----------------
  // ضع معرف Google Analytics هنا (مثال: G-XXXXXXXXXX). اتركه فارغًا للتعطيل.
  googleAnalyticsId: "",

  // كود تحقق Google Search Console (من صفحة إعدادات الملكية -> HTML tag)
  googleSiteVerification: "",

  // معرف تحويل Google Ads (اختياري، لاحقًا)
  googleAdsConversionId: "",
};

/**
 * ============================================================
 *  إعدادات مصدر أسعار الذهب
 * ============================================================
 * لا يوجد API حقيقي بعد. عندما تحصل على مصدر بيانات حقيقي
 * (مثل GoldAPI.io أو أي مزود آخر) ضع رابط الـ API والمفتاح هنا،
 * وفعّل isLive = true.
 *
 * إلى أن يحدث ذلك، سيعرض الموقع رسالة واضحة للمستخدم بدل بيانات وهمية.
 */
export const goldPriceConfig = {
  isLive: false, // غيّرها إلى true بعد ربط API حقيقي
  apiUrl: "", // مثال: "https://api.example.com/v1/gold"
  apiKey: "", // مفتاح الـ API الخاص بك
  // كل كم دقيقة يتم تحديث السعر (لأغراض العرض فقط حاليًا)
  refreshMinutes: 15,
};

/**
 * ============================================================
 *  روابط Affiliate لكل متجر
 * ============================================================
 * إذا تركت الرابط فارغًا "" فلن يظهر زر "زيارة عبر الشريك" للمستخدم،
 * وسيظهر بدلاً منه رابط الموقع الرسمي العادي فقط.
 * بمجرد إضافة الرابط هنا، سيظهر الزر تلقائيًا في كل الصفحات ذات الصلة
 * (بطاقة المقارنة، صفحة المتجر، صفحة الهبوط /go/[slug]).
 */
export const affiliateLinks: Record<string, string> = {
  // ضع رابط Affiliate هنا لكل متجر (بدون حذف السطر، فقط عدّل القيمة)
  zomorod: "", // AFFILIATE_URL لمتجر زمرد
  alrabsh: "", // AFFILIATE_URL لمتجر مصاغات الأربش
  "colin-cleef": "", // AFFILIATE_URL لمتجر كولن كليف
  alghunaim: "", // AFFILIATE_URL لمتجر مجوهرات الغنيم
  "onyx-rose": "", // AFFILIATE_URL لمتجر أونيكس روز
};
